# bug-triage
bug_triage is a developer CLI tool that ingests open GitHub issues or raw bug report files and uses an LLM (OpenAI or Anthropic) to automatically classify, deduplicate, and prioritize them by severity and impact. It produces a structured triage report in Markdown or JSON format, complete with suggested fix order, estimated complexity scores, groupi
